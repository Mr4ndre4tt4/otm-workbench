# Assets Linked Target Search UI Plan

## Goal

Expose linked target type filtering on `/assets/library`.

## Steps

1. Add a failing functional test for linked target type filter controls.
2. Extend frontend asset filter types.
3. Render linked target type and operator controls in the library search panel.
4. Update browser QA to exercise reset and apply behavior.
5. Run focused/full Assets tests, build, browser QA, and diff checks.
